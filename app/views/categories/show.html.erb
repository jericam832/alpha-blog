<div class="jumbotron" id="cat-head">
    <div class="container">
        <h1 class="page-title"><%= "Category: " + @category.name %> </h1>
        
        <% if logged_in? and current_user.admin? %>
            <div class="row">
                <span class="badge"><%= link_to "Edit Category Name", edit_category_path(@category) %></span>   
            </div>
        <% end %>
        <p>Here are all of the articles tagged as <%= @category.name %>.</p>
      
    </div>
</div>
<div align="center">
    <%= will_paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles %>

<div align="center">
    <%= will_paginate @category_articles %>
</div>